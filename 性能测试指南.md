# 性能测试指南

starnet 的性能测试方法对齐 skynet 的官方套路（`test/testecho.lua`、debug STAT、外部网络压测），按**三层**分别测：

| 层 | 测什么 | 对应文件 | 关键指标 |
|---|---|---|---|
| 引擎核心 | 消息队列 + worker 分发 + 协程 RPC | `examples/benchmark.lua` | RPC qps |
| 网络 | TCP 收发吞吐 | `examples/echo.lua` + 外部客户端 | MB/s、并发连接 |
| 服务能力 | 定时器精度、每服务统计 | `examples/benchmark_timer.lua` | 延迟分布、cpu/message |

> 前置：按根 `README.md` 构建好 `build/starnet`，且**必须在 `build/` 目录运行**（服务路径相对运行目录）。

---

## 一、进程内 RPC 基准（核心）

复刻 skynet `test/testecho.lua`：一个 master 服务创建 echo 从服务，串行 / 并行发起 `starnet.call`，用 `starnet.now()`（厘秒）计时。

**文件**：
- `examples/benchmark_echo.lua` —— 从服务：`dispatch("lua")` 收到即 `starnet.ret` 回显。
- `examples/benchmark.lua` —— master：串行 100000 次 call + 并行（10 协程 × 10000 次）call，末尾打印本服务 `STAT`。
- `examples/config_bench.lua` —— `thread=8`、`start="benchmark"`、`profile=true`。

**跑法**：
```sh
cd build
./starnet ../examples/config_bench.lua
# 跑完打印结果后进程不退出（服务常驻），Ctrl+C 优雅停机
```

**预期输出**（i5-12600KF / 16 线程 / release 实测）：
```
benchmark slave = 2
sequential: 100000 calls, 151 cs, qps = 66225
self STAT: message=100000 cpu=0.0000s mem=5000.0KB
parallel (workers=10): 100000 calls, 20 cs, qps = 500000
```

**判据 / 怎么看**：
- **qps = calls / (now_end - now_start) × 100**（`starnet.now()` 单位厘秒）。
- 串行 qps = 单协程 round-trip 延迟的倒数；并行 qps 随 worker 数提升（本条机器约 7~8 倍），反映多 worker 叠加的吞吐上限。
- `self STAT.message` ≈ 已完成的 call 数（每个 call 对 master 而言是一条 RESPONSE）；若两次测的数字低一个量级，检查是否 `profile=true` 叠加、或机器负载。
- **量级参考**：本引擎（简化教学版，每条消息经锁 + 拷贝 + 协程创建）在 i5-12600KF 上串行 ~6 万、并行 ~50 万 qps；skynet 同类机器并行约 100~200 万 qps。差距是设计使然（见根 `missing-skynet-modules.md`），本测试用于**发现引擎自身的回归**，不比绝对值。

**调参**：
| 想测 | 改法 |
|---|---|
| 纯吞吐（去掉 profile 开销） | `config_bench.lua` 里 `profile = false` |
| worker 缩放 | 改 `thread`（对齐 skynet 惯例测 4/8/16） |
| 更大样本 | 改 `benchmark.lua` 里的 `N = 100000` |

---

## 二、TCP echo 网络基准

**文件**：
- `examples/echo.lua` —— `socket.listen(8004)` + `rawdata()` 裸字节流，`dispatch("socket")` 原样回写。
- `examples/config_echo.lua` —— `start="echo"`（端口可 env `echo_port` 覆盖）。

**跑法**（两个终端）：
```sh
# 终端 1：起 echo
cd build && ./starnet ../examples/config_echo.lua
# 终端 2：并发吞吐压测
python3 - <<'EOF'
import socket, time, threading
CONNS, ITERS, SIZE = 8, 2000, 4096
payload = b"x" * SIZE
tot = [0.0]; lock = threading.Lock(); ok = [0]
def worker(cid):
    s = socket.create_connection(("127.0.0.1", 8004))
    n = 0
    for _ in range(ITERS):
        s.sendall(payload)
        got = 0
        while got < SIZE:
            d = s.recv(SIZE - got)
            if not d: break
            got += len(d)
        n += got
    s.close()
    with lock:
        tot[0] += n; ok[0] += 1
start = time.time()
ts = [threading.Thread(target=worker, args=(i,)) for i in range(CONNS)]
[t.start() for t in ts]; [t.join() for t in ts]
el = time.time() - start
print(f"TCP echo: {CONNS} conns x {ITERS} x {SIZE}B -> {tot[0]/1024/1024/el:.1f} MB/s, {ok[0]}/{CONNS}, {el:.2f}s")
EOF
```

**预期输出**（loopback 实测）：`8 conns x 2000 x 4096B -> 92.0 MB/s`，全部连接成功。

**判据 / 怎么看**：
- 吞吐上不去先看**单连接是否写缓冲积压**：服务日志出现 `dispatch("warning")` 说明写缓冲堆积（回写跟不上发送端），属发送端过快，可加大包/减少并发，或检查是否误开了 `nodelay`/`pause`。
- 用 `starnet.socket.info(fd)` 查看单连接 `wbuffer`。
- 压测前 `ulimit -n` 调高（`ulimit -n 65535`），否则并发连接在 OS 层被拒（服务日志 `accept` 后立刻 `close`）。

---

## 三、定时器精度

**文件**：`examples/benchmark_timer.lua` —— 1000 个并发协程 `starnet.sleep(1)`（10ms），统计实际延迟的 avg/max。

**跑法**（临时改 `config_bench.lua` 的 `start = "benchmark_timer"`）：
```sh
cd build && ./starnet ../examples/config_bench.lua
```

**预期输出**（实测）：
```
timer: 1000 sleep(1), avg=1.00cs max=1cs wall=1cs
```

**判据**：timer 线程每 2.5ms 驱动（`StarnetStart::TimerLoop`），`sleep(1)` 应基本贴合目标；`max` 明显偏大（如几十 cs）说明服务消息队列被长任务占住，定时器精度受消息处理阻塞影响——这是单服务单线程模型（对齐 skynet）的固有行为。

---

## 四、观测指标：现状与缺口

**已有**（查**当前服务**自己的统计）：
- `starnet.cpu()` / `starnet.message()` / `starnet.msgtime()` —— 累计 CPU 秒 / 消息数 / 当前消息耗时（需 `profile=true`，见 `lua-starnet.cpp`）。
- `starnet.command("STAT")` —— 返回 `{cpu, time, message}` 表。
- `starnet.mem()` —— 进程 RSS KB；`starnet.now()` / `starttime()` / `time()` 计时。

**缺口**（对标 skynet `debug` 协议 + launcher 聚合）：
- 只能查**自己**，不能查任意服务的统计（skynet 用 `debug STAT/INFO` + `.launcher` LIST 聚合）。
- 基准阶段在单服务内测即可覆盖；跨服务统计/console 聚合归入**"单服务生命周期 + 可观测性"落地方案**（待做），不阻塞本测试。

---

## 五、外部工具（辅助定位）

| 工具 | 用法 | 看什么 |
|---|---|---|
| `perf stat` / `perf top` | `perf stat -e task-clock,context-switches ./starnet ...` | worker 线程 CPU 占比、上下文切换（锁竞争信号） |
| `pidstat -t -p <pid> 1` | 观察各线程 CPU | 多 worker 是否打满、socket/timer 线程占比 |
| `top -H -p <pid>` | 线程级 CPU | 同上 |
| `valgrind --tool=callgrind` | 压测时采样 | C++/Lua 调用分布、热点 |
| `strace -c` | 统计系统调用 | 确认无异常 syscall（如每消息读时钟） |

---

## 六、常见问题

| 现象 | 原因 / 处理 |
|---|---|
| 跑完打印结果后不退出 | 正常——服务常驻，`Ctrl+C` 优雅停机（会打印各服务 `OnExit`） |
| 并行 qps 和串行差不多 | 检查 `thread` 是否 ≥ 2 且 ≥ worker 数；`N` 是否太小（单轮调度噪声大） |
| `STAT.cpu` 恒 0 | `config` 里 `profile = false`；开 `profile = true` 才有 CPU 统计 |
| echo 压测连接被拒 | `ulimit -n` 太低，先 `ulimit -n 65535` |
| 日志出现 `warning` 子类型 | 写缓冲积压告警（≥1MB 起），说明发送端过快或写线程忙 |
| 数字比 skynet 低一个量级 | 设计使然（简化版引擎），用本机多次运行取 min/median 看相对回归 |