# 代码阅读指南

> 本指南面向首次接触 starnet 源码的读者，按「启动 → 消息 → 服务 → 网络 → 基础设施」的顺序给出阅读路线，标注每个阶段需要理解的核心概念与关键代码位置。

## 0. 全局认知

starnet 是一个 **C++ 内核 + Lua 服务** 的两层架构，整体对齐 skynet：

```
┌─────────────────────────────────────────────────────────────┐
│  Lua 层（业务）                                              │
│   lualib/starnet.lua        协程调度 / session RPC 宿主库     │
│   examples/*.lua            服务脚本（dispatch 回调）         │
│   lualib/starnet/           cluster.lua / sharedata.lua      │
├─────────────────────────────────────────────────────────────┤
│  C 桥接层（绑定）                                            │
│   lualib-src/lua-starnet.cpp    C API 绑定（starnet 全局表）  │
│   lualib-src/lua-netpack.cpp    粘包/半包解析                 │
│   lualib-src/lua-seri.cpp       序列化                       │
│   lualib-src/lua-sharedata.cpp  共享只读数据 C 绑定           │
├─────────────────────────────────────────────────────────────┤
│  C++ 内核（框架）                                            │
│   Starnet::Send / NewService     服务管理                     │
│   StarnetMQ + 全局队列           消息传输                     │
│   Worker 线程池                  消息调度                     │
│   SocketServer / Timer / Monitor 系统线程                     │
└─────────────────────────────────────────────────────────────┘
```

**最重要的心智模型**：整框架是一台「**消息分发机器**」——生产者（socket 线程 / timer 线程 / 任意服务）把消息塞进目标服务的二级队列，worker 线程从全局队列取出服务，批量消费其队列消息，把消息翻译成对 Lua 协程的调用。

## 1. 阅读路线总览

| 阶段 | 主题 | 文件 | 重点概念 |
| --- | --- | --- | --- |
| ① | 入口与启动 | `src/starnet_main.cpp`、`src/starnet.cpp`、`src/starnet_start.cpp` | 线程模型、单例、优雅退出 |
| ② | 消息模型 | `include/starnet_msg.h`、`src/starnet_mq.cpp` | 消息类型体系、二级+全局队列 |
| ③ | Worker 调度 | `src/starnet_worker.cpp` | weight 加权调度、退出安全点 |
| ④ | 服务与 Lua 桥接 | `src/starnet_service.cpp`、`lualib-src/lua-starnet.cpp` | 每服务一个 Lua 虚拟机 |
| ⑤ | Lua 宿主库 | `lualib/starnet.lua` | 协程池、session RPC、dispatch |
| ⑥ | 网络层 | `src/starnet_socket_server.cpp`、`src/starnet_socket.cpp` | epoll、读写分工、写缓冲 |
| ⑦ | 基础设施 | `src/starnet_timer.cpp`、`src/starnet_handle.cpp`、`starnet_env/logger/mem/monitor/daemon` | 时间轮、名字服务 |
| ⑧ | 扩展能力 | `lua-netpack.cpp`、`lua-seri.cpp`、`lua-sharedata.cpp`、`starnet_sharedata.cpp`、`lualib/starnet/cluster.lua` | 封包/序列化/共享数据/跨节点 |

> 代码中的注释大量标注了「对齐 skynet 的 xxx」，这是本项目最大的特点——**每个模块都有一个 skynet 参照物**。熟悉 skynet 的读者可以直接按注释跳到对应 skynet 源码对照理解；不熟悉的读者可忽略这层对应关系，直接理解注释所描述的行为。

## 2. 阶段①：入口与启动

### 2.1 入口

`src/starnet_main.cpp:29` 的 `main` 极短，只有四步：

1. `StarnetConfig::Load(argv[1])` —— 加载配置文件（`src/starnet_config.cpp:27`，用 Lua 解释器执行 config.lua，提取顶层键）
2. `new Starnet()` —— 初始化单例
3. `Starnet::inst->Start(cfg)` —— 启动框架
4. `Starnet::inst->NewService(cfg.start)` —— 拉起第一个服务（默认 `main`）
5. `Starnet::inst->Wait()` —— 阻塞直到优雅退出

### 2.2 启动流程

`src/starnet.cpp:41` `Starnet::Start` 是框架的初始化主线，**顺序敏感**：

```
daemon 化（fork 必须在任何线程创建前）
→ env 初始化（config 全部键导入）
→ logger 初始化
→ 忽略 SIGPIPE / 注册 SIGINT/SIGTERM 信号 handler
→ globalmq / handle（名字服务）/ timer 初始化
→ profile 开关
→ new StarnetStart → Start()   ← 创建全部系统线程
```

`src/starnet_start.cpp:92` `StarnetStart::Start` 创建 4 类线程（对齐 skynet 的线程模型）：

| 线程 | 数量 | 职责 | 代码位置 |
| --- | --- | --- | --- |
| Worker | `config.thread`（默认 8） | 消费消息、执行 Lua 服务逻辑 | `src/starnet_start.cpp:28` |
| Socket | 1 | epoll 事件循环，统一读 TCP/UDP | `src/starnet_start.cpp:59` |
| Timer | 1 | 每 2.5ms 驱动时间轮 | `src/starnet_start.cpp:82` |
| Monitor | 1 | 每 5 秒检查 worker 是否卡死 | `src/starnet_start.cpp:87` |

**值得注意**：每个服务拥有**独立的 `lua_State`**（见阶段④），Lua 层无锁并发由「单服务单 worker 同时处理」保证。

### 2.3 优雅退出

`src/starnet.cpp:94` `Wait` → `StarnetStart::Wait`（`src/starnet_start.cpp:104`）：

```
等待退出请求（SIGINT/SIGTERM 或 Lua 侧 starnet.globalexit）
→ KillAllServices：所有服务退休（触发 Lua OnExit + lua_close + 丢弃残留消息）
→ SetExit + WakeUpAll：置退出标志并广播唤醒休眠 worker
→ join 全部线程（worker → timer → socket → monitor）
```

信号处理只是置位一个 `volatile sig_atomic_t` 标志（`src/starnet.cpp:36`），主线程 `Wait` 轮询——**停机不在信号上下文执行**，保证 async-signal-safety。

## 3. 阶段②：消息模型

这是框架的「血液循环系统」，建议最先吃透。

### 3.1 消息类型体系 `include/starnet_msg.h`

- `BaseMsg`：抽象基类，唯一字段 `type`（枚举对齐 skynet 的 `PTYPE_*`，`LUA=10`、`SOCKET=6`、`RESPONSE=1`、`ERROR=7` 等）
- `ServiceMsg : BaseMsg`：服务间消息，字段 `source`（发送方 handle）、`session`（会话号，`0` 表示无需响应）、`buff`（内容）
- `SocketMsg : BaseMsg`：网络消息，`subtype`（对齐 `SKYNET_SOCKET_TYPE_*`：`DATA/ACCEPT/CLOSE/CONNECT/ERROR/UDP/WARNING`）+ `fd` + `buff` + `udpAddr`

> 类型不变量：`type` 字段与派生类一一对应——`type==SOCKET` 是 `SocketMsg`，其余是 `ServiceMsg`。`src/starnet_worker.cpp:12` 的 `dropMessage` 依赖此不变量做 `static_pointer_cast`。

### 3.2 二级队列 + 全局队列 `src/starnet_mq.cpp`

这是消息传输的核心机制：

```
发送方                              Worker 线程
  │                                    │
  ▼                                    ▼
服务 mq（StarnetMQ，每服务一个）→ TryEnterGlobal → 全局队列 → starnet_globalmq_pop
  └──── 自旋锁保护 ──────┘      ┌──── 自旋锁保护 ────┘
```

- `StarnetMQ`（每服务一个，`src/starnet_mq.cpp:19` `Push`）：存该服务的全部消息
- `TryEnterGlobal`（`src/starnet_mq.cpp:104`）：服务**不在**全局队列时才入队并返回 true（需要唤醒 worker）；已在队列则跳过
- `FinishDispatch`（`src/starnet_mq.cpp:119`）：worker 处理完一批后，队列非空则**重新入全局队列**，空则置 `inGlobal=false`

**这套设计的价值**：同一服务一旦被某 worker 处理，其余 worker 不会再来争抢（`inGlobal` 保证全局队列里同一服务只有一个副本），从而**避免了同一服务被并发处理**。

`Overload` 告警（`src/starnet_mq.cpp:63`）：pop 时剩余长度超过阈值（1024）则记录，阈值指数倍增（1024→2048→4096…），由 worker 检测后打日志。

## 4. 阶段③：Worker 调度 `src/starnet_worker.cpp`

`Worker::operator()`（`src/starnet_worker.cpp:51`）是每个 worker 线程的主循环：

```
while true:
  srv = starnet_globalmq_pop()
  空：
    退出标志置位 → break（优雅退出）
    否则 → WorkerWait() 休眠（被 CheckAndWeakUp 唤醒）
  非空：
    monitor_trigger（卡死监视）
    weight 调度：weight<0 处理 1 条；>=0 处理 队列长度>>weight 条
    srv->ProcessMsgs(n)        ← 消费消息，执行 Lua
    monitor_check
    CheckAndPutGlobal(srv)     ← 决定重新入队 / 置 inGlobal=false / 执行退出
```

### 4.1 weight 加权调度

`src/starnet_start.cpp:30` 硬编码 weight 表（对齐 skynet）：前 4 个 worker `-1`（每轮 1 条），5~8 个 `0`（每轮 `长度>>0` = 全量），之后每 8 个一组递增。**效果**：worker 数 ≤4 时全部每轮 1 条，调度效率低——这就是 `config.thread` 默认取 8 的原因。

### 4.2 退出安全点

`CheckAndPutGlobal`（`src/starnet_worker.cpp:34`）是服务生命周期中最关键的函数：

```
if srv->isExiting:
    if !srv->exited.exchange(true):    // 只执行一次
        srv->OnExit()                  // 调 Lua OnExit + lua_close
        srv->mq.Clear(dropMessage)     // 丢弃残留，回 PTYPE_ERROR 通知发送方
```

服务退休（`KillService`，`src/starnet.cpp:132`）只是把服务从名字表/服务表摘除并标记 `isExiting`，真正的 `lua_close` 延迟到 **worker 线程在这个安全点执行**——避免「调用线程同步 `lua_close` 与 worker 并发执行 Lua」的崩溃风险。

`dropMessage`（`src/starnet_worker.cpp:12`）在丢弃请求消息时向发送方回一个 `PTYPE_ERROR`，让正在 `starnet.call` 的协程报错退出。

## 5. 阶段④：服务与 Lua 桥接

### 5.1 服务对象 `include/starnet_service.h`

`Service` 是框架的「服务单元」，字段集中在头文件：
- `id`：handle（从 1 开始，0 保留）
- `mq`：该服务的二级队列
- `sessionGen`：RPC 会话号自增器
- `profile / cpuCost / messageCount`：性能统计
- `luaState`：**每服务一个独立 Lua 虚拟机**

### 5.2 服务初始化 `Service::OnInit`（`src/starnet_service.cpp:107`）

```
new lua_State → luaL_openlibs
→ LuaAPI::Register（注册 C API 为全局 starnet 表）
→ 设置 package.path（lualib 路径）
→ require "starnet"（加载宿主库，覆盖全局 starnet 表）
→ 注册表存 Service 指针（C 绑定通过它取当前服务上下文）
→ 按 luaservice 模板查找 <type>.lua 并执行
```

### 5.3 消息进入 Lua 的路径

`Service::OnMsg`（`src/starnet_service.cpp:268`）按 `type` 分流：
- `LUA`/`RESPONSE` → `OnServiceMsg`（`src/starnet_service.cpp:177`）→ 调 `dispatch_message`
- `SOCKET` → `OnSocketMsg`（`src/starnet_service.cpp:193`）→ 按 `subtype` 翻译成 Lua 参数 → 调 `dispatch_socket`

`CallStarnetLua`（`src/starnet_service.cpp:83`）是 C→Lua 的唯一入口：取全局 `starnet` 表 → 取字段 → 参数排栈 → `lua_pcall`。

### 5.4 C API 绑定 `lualib-src/lua-starnet.cpp`

`LuaAPI::Register`（`src/lualib-src/lua-starnet.cpp:23`）注册全部 C 函数到一个 `luaL_Reg` 表，并挂上 `netpack` / `sharedata` 两个子表。重点看两个：

- `SendSession`（`src/lualib-src/lua-starnet.cpp:172`）：核心发送原语——参数 `(toId, type, session, buff)`，`source` 从注册表取当前服务
- `GetCurrentService`（`src/lualib-src/lua-starnet.cpp:15`）：从 `LUA_REGISTRYINDEX` 的 `starnet_service` 取出 Service 指针

> 阅读 C 绑定层时注意一个**内存约定**：`Write`（`:265`）用 `new char[]` 拷贝 Lua 字符串后交给 `shared_ptr<char>(..., default_delete<char[]>())` 传递——因为 `SocketServer::SendBuffer` 是异步的（写缓冲），必须拷贝出 Lua GC 管理的字符串。

## 6. 阶段⑤：Lua 宿主库 `lualib/starnet.lua`

这是「Lua 侧框架」，建议对照 C 层理解。核心机制：

### 6.1 协程池

`co_create`（`starnet.lua:112`）：复用协程而非每次新建。池中协程被改造成循环执行器——主函数跑完后 `yield("SUSPEND")` 等待新主函数。

### 6.2 session RPC 的三张表

```
session_id_coroutine       session → 等待恢复的协程
session_coroutine_id       co → 当前协程的 session
session_coroutine_address  co → 当前协程的 source（请求方）
```

`starnet.call`（`starnet.lua:473`）的完整链路：

```
call(addr, "lua", ...)
→ resolve_addr（'.名字' → handle）
→ session = c.genid()                      ← 分配会话号
→ c.send_session(addr, PTYPE_LUA, session, msg)
→ yield_call：session_id_coroutine[session]=当前协程; coroutine.yield
    ┌───────────────────────────────────────────────┐
    │ 对端 dispatch("lua", session, source, buff)    │
    │  → starnet.ret(msg)                            │
    │  → c.send_session(co_address, RESPONSE, session, msg) │
    └───────────────────────────────────────────────┘
→ 收到 RESPONSE：raw_dispatch_message 找到 session 对应协程 resume
→ 协程被唤醒，yield_call 返回 → p.unpack(...)
```

`starnet.ret`（`starnet.lua:483`）从当前协程的 `session_coroutine_id` 取会话号回包；`starnet.response`（`:501`）返回**一次性**异步回包函数。

### 6.3 消息分发入口

`raw_dispatch_message`（`starnet.lua:159`）按类型处理：
- `RESPONSE`：`session_id_coroutine[session]` 有则 resume（等待的 call/sleep）
- `ERROR`：resume 但传 `false`，让 `yield_call` 抛 "call failed"
- 普通消息：`co_create(p.dispatch)` 新协程执行，参数 `(session, source, unpack(buff))`

`dispatch_socket`（`starnet.lua:231`）把 C 侧传来的 socket 子类型翻译回 `dispatch("accept"/"socket"/"close"/...)` 回调，其中 **`socket` 数据分支接入 `netpack.filter` 解析粘包/半包**（见阶段⑧）。

## 7. 阶段⑥：网络层

### 7.1 线程分工（本项目最核心的设计决策）

```
socket 线程：  epoll_wait → 读 TCP/UDP → 产出 SocketMsg → 投进目标服务 mq
worker 线程：  ProcessMsg → OnSocketMsg → dispatch_socket → Lua 回调
```

「**读统一到 socket 线程**」：`ReadData`（`src/starnet_socket_server.cpp:224`）循环 `read`、`ReadUdp`（`:274`）循环 `recvfrom`，每块/每包投一条消息；worker 侧 `OnSocketMsg` 直接用现成数据，**不再碰 `read/recvfrom`**。EOF/错误检测也随读迁移（`read==0 → CLOSE`）。

### 7.2 事件循环 `src/starnet_socket_server.cpp:361`

```
epoll_wait（阻塞，被 eventfd 唤醒）
→ OnEvent（:325）：按 fd 查 Conn
   LISTEN → OnAccept（循环 accept 到 EAGAIN，ET 模式必须清空）
   普通   → OnRW（:151）：可写→刷写缓冲/检查 connect；可读→ReadData/ReadUdp
→ 退出标志 → CloseAll 收尾
```

### 7.3 写缓冲优先级

`SendBuffer`（`src/starnet_socket_server.cpp:590`，worker 线程调用）+ `OnWriteable`（`:641`，socket 线程刷写）：
- 缓冲空时先直写，写不完的进 **high 队列**（`objs`）
- 有积压时按优先级入队：high（`starnet.socket.write`）/ low（`write_low`）
- 刷写顺序（对齐 skynet `send_buffer_`）：**刷完 high → 刷 low → low 头半包挪到 high 尾防 TCP 乱序 → 都空关 EPOLLOUT**
- 积压告警：`wbSize ≥ 1MB` 且 `≥ warnSize` 时投 `WARNING`，阈值翻倍渐进（`1MB→2MB→4MB…`），由业务决定是否踢连接

### 7.4 主动连接 / 绑定 fd / UDP

- `Connect`（`:855`）：非阻塞 `connect`，立即成功或 `EINPROGRESS` 等 `EPOLLOUT` → `OnConnectFinish`（`:175`）`getsockopt(SO_ERROR)` 检查，成功投 `CONNECT`、失败投 `ERROR`
- `Bind`（`:930`）：接管外部 fd，`getsockopt(SO_TYPE)` 自动识别 TCP/UDP，**引擎不负责 close**
- UDP：`AddUdp/SetUdpAddress/SendUdp`（`:754` 起），报式直发 `sendto`，无写缓冲

### 7.5 桥接层 `src/starnet_socket.cpp`

极简（6 行）：`SocketBridge::OnSocketMsg` → `Starnet::inst->Send(serviceId, msg)`。它是引擎与 Starnet 之间的解耦点——`SocketServer` 不依赖 `Starnet`。

## 8. 阶段⑦：基础设施

| 模块 | 文件 | 要点 |
| --- | --- | --- |
| 定时器 | `src/starnet_timer.cpp` | 4 级时间轮（`TIME_NEAR` + `t[4]`），timer 线程每 2.5ms 调 `starnet_updatetime`（`:227`）；`starnet_timeout`（`:190`）注册后到期投 `RESPONSE` 消息到目标服务——**Lua 的 sleep/fork/timeout 全部建立在这之上** |
| 名字服务 | `src/starnet_handle.cpp` | `unordered_map + rwlock`：`namehandle/findname/removename`；handle 高 8 位预留给 harbor（单节点为 0） |
| 环境配置 | `src/starnet_env.cpp` | `unordered_map + rwlock`，返回 `string` 拷贝规避指针生命周期问题 |
| 日志 | `src/starnet_logger.cpp` | 单例 + 互斥锁，时间戳格式 `HH:MM:SS.mmm`，支持文件/stderr |
| 内存统计 | `src/starnet_mem.cpp` | 读 `/proc/self/status` 的 `VmRSS`（进程 RSS） |
| 监视器 | `src/starnet_monitor.cpp` | 每 worker 一个 monitor：处理消息前 `trigger`（版本号+1）、批后 `check`；monitor 线程 5 秒比较版本号，发现「触发未完成」打卡死告警 |
| 守护进程 | `src/starnet_daemon.cpp` | `check_pid → daemon(1,1) → flock 写 pidfile → stdio 重定向 /dev/null` |
| 配置 | `src/starnet_config.cpp` | 用临时 Lua 状态执行 config.lua，提取 `luaservice/start/thread/luaPath/logger/daemon/profile` + 全部顶层标量键入 env |

## 9. 阶段⑧：扩展能力

| 模块 | 文件 | 说明 |
| --- | --- | --- |
| 粘包/半包 | `lualib-src/lua-netpack.cpp` | `netpack.create/filter/pop/pack/close`：按 fd 维护 `uncomplete` 半包链表，按 2 字节大端长度头拆包；`starnet.pack` 加长度头 |
| 序列化 | `lualib-src/lua-seri.cpp` | `serialize/unserialize/packstring/unpackstring`；数字压缩 + 嵌套表支持；**返回字符串**（skynet 返回 userdata，starnet 简化由 GC 自管） |
| 共享数据 | `src/starnet_sharedata.cpp` + `lualib-src/lua-sharedata.cpp` + `lualib/starnet/sharedata.lua` | 跨服务零拷贝只读表：紧凑只读表（数组段+哈希段）+ 全局 `name→TableState` + 版本号；box userdata 的 `__index` 直查（不走消息队列），`__gc` 引用计数延迟回收；`sharedata.lua` 提供 `query/new/update/delete/deepcopy/subscribe`（subscribe 为轮询简化版） |
| 跨节点 RPC | `lualib/starnet/cluster.lua` + `examples/clusterd.lua` | 全 Lua 实现：每节点一个 clusterd 服务管理出站/入站连接，`@名字` 解析，`cluster.call/send/query/register`；协议为 `starnet.pack` 长度头 + 内部 `>s2i4s4`/`>i4i1s4` 编码 |

## 10. 推荐调试与验证路径

1. **最小链路验证**：从 `examples/ping.lua` 出发——`main.lua` 里 `starnet.call(".ping",...)`，跟踪一次 RPC 全链路（send_session → mq → worker → dispatch_message → ret → RESPONSE 恢复协程），覆盖阶段③④⑤全部核心。
2. **网络验证**：`examples/chat.lua` 的广播——覆盖阶段⑥⑦（accept → dispatch_socket → netpack.filter → write + pack）。
3. **断点建议**：`Starnet::Send`（所有消息必经）、`Worker::operator()`（调度）、`Service::OnServiceMsg`（C→Lua 转换）、`starnet.lua` 的 `raw_dispatch_message`。
4. **理解阻塞模型**：Lua 里 `starnet.call` 挂起的是协程而非 worker 线程——服务在处理一条消息时 sleep/call，worker 可以切去处理别的服务。这是框架吞吐的核心，务必通过调试确认「worker 线程栈 + 协程栈」的分层。

## 11. 常见坑位速查

- `shared_ptr<char>` 的数组 deleter：拷贝 Lua 字符串必须用 `default_delete<char[]>`，否则 `delete` 数组是 UB（`lua-starnet.cpp:286`）。
- 写缓冲是异步的：`starnet.socket.write` 返回不代表对端已收到，积压/告警是正常机制而非 bug。
- `TryEnterGlobal` 的返回值的语义是「是否首次入队（需要唤醒）」——它是 `Send` 后是否 `CheckAndWeakUp` 的依据（`starnet.cpp:223`）。
- 服务退出不立刻销毁：`KillService` 只标记，worker 在安全点执行 `OnExit`/`lua_close`（`starnet_worker.cpp:38`）。
- ET 模式下 `accept` 必须循环到 EAGAIN，否则漏连接（`starnet_socket_server.cpp:109`）。
- `config.thread ≤ 4` 时 weight 全为 -1，调度退化为每轮 1 条（`starnet_start.cpp:30`）。
